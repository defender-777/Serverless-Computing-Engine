const event=JSON.parse(process.argv[2]); exports.handler=async(event)=>{ const prices=event.prices; const window=event.window; const result=[]; for(let i=0;i<=prices.length-window;i++){ const avg=prices.slice(i,i+window).reduce((a,b)=>a+b,0)/window; result.push(avg);} return result;}; (async()=>{const result=await exports.handler(event); console.log(JSON.stringify(result));})();