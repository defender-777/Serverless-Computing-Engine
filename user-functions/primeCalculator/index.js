const event = JSON.parse(process.argv[2]); function isPrime(n){ if(n<=1) return false; for(let i=2;i<=Math.sqrt(n);i++){ if(n%i===0) return false;} return true;} exports.handler=async(event)=>{ const limit=event.limit; const primes=[]; for(let i=2;i<=limit;i++){ if(isPrime(i)) primes.push(i);} return {count:primes.length,primes};}; (async()=>{ const result=await exports.handler(event); console.log(JSON.stringify(result));})();