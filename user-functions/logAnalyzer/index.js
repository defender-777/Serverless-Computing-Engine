const event=JSON.parse(process.argv[2]); exports.handler=async(event)=>{ const logs=event.logs; const count={}; logs.forEach(l=>{count[l]=(count[l]||0)+1}); return count;}; (async()=>{const result=await exports.handler(event); console.log(JSON.stringify(result));})();